## 🟦 Задание 2.1 (тематика: **Отдел продаж**)

### Условие

1. Создайте DataFrame с данными о сотрудниках отдела продаж:

   * ФИО;
   * Возраст;
   * Опыт (категориальная переменная: "стажёр", "младший", "средний", "старший").
2. Преобразуйте столбец **Опыт** в упорядоченную категориальную переменную с порядком: стажёр → младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Опыту** (от стажёра к старшему),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Бонус**:

   * значения — случайные целые числа от **5000 до 20000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * максимальный и минимальный бонус,
   * среднее значение возраста.

---

## 🟦 Задание 2.2 (тематика: **IT-отдел**)

### Условие

1. Создайте DataFrame с данными о сотрудниках IT:

   * Имя;
   * Возраст;
   * Уровень (категориальная переменная: "junior", "middle", "senior").
2. Преобразуйте столбец **Уровень** в упорядоченную категориальную переменную с порядком: junior → middle → senior.
3. Отсортируйте сотрудников:

   * сначала по **Уровню** (junior → senior),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Проектная премия**:

   * значения — случайные целые числа от **2000 до 10000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * общее количество сотрудников каждого уровня,
   * среднюю премию.

---

## 🟦 Задание 2.3 (тематика: **Бухгалтерия**)

### Условие

1. Создайте DataFrame с данными бухгалтеров:

   * ФИО;
   * Возраст;
   * Квалификация (категориальная переменная: "junior", "middle", "senior").
2. Преобразуйте столбец **Квалификация** в упорядоченную категориальную переменную с порядком: junior → middle → senior.
3. Отсортируйте сотрудников:

   * сначала по **Квалификации** (junior → senior),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Надбавка**:

   * значения — случайные целые числа от **1000 до 8000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * сумму надбавок,
   * средний возраст.

---

## 🟦 Задание 2.4 (тематика: **Отдел маркетинга**)

### Условие

1. Создайте DataFrame с данными сотрудников маркетинга:

   * ФИО;
   * Возраст;
   * Должность (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Должность** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Должности** (младший → старший),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Бонус за KPI**:

   * значения — случайные целые числа от **3000 до 15000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * максимальный и минимальный возраст,
   * среднее значение бонуса.

---

## 🟦 Задание 2.5 (тематика: **Отдел кадров**)

### Условие

1. Создайте DataFrame с данными сотрудников отдела кадров:

   * ФИО;
   * Возраст;
   * Стаж (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Стаж** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Стажу** (младший → старший),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Премия к зарплате**:

   * значения — случайные целые числа от **1000 до 10000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * количество сотрудников каждого стажа,
   * сумму премий.

---

## 🟦 Задание 2.6 (тематика: **Производственный отдел**)

### Условие

1. Создайте DataFrame с данными сотрудников производственного отдела:

   * ФИО;
   * Возраст;
   * Ранг (категориальная переменная: "ассистент", "специалист", "руководитель").
2. Преобразуйте столбец **Ранг** в упорядоченную категориальную переменную с порядком: ассистент → специалист → руководитель.
3. Отсортируйте сотрудников:

   * сначала по **Рангу** (ассистент → руководитель),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Премия за эффективность**:

   * значения — случайные целые числа от **2000 до 12000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * минимальный и максимальный ранг,
   * среднюю премию.

---

## 🟦 Задание 2.7 (тематика: **Отдел логистики**)

### Условие

1. Создайте DataFrame с данными сотрудников отдела логистики:

   * ФИО;
   * Возраст;
   * Уровень (категориальная переменная: "junior", "middle", "senior").
2. Преобразуйте столбец **Уровень** в упорядоченную категориальную переменную с порядком: junior → middle → senior.
3. Отсортируйте сотрудников:

   * сначала по **Уровню** (junior → senior),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Бонус за выполнение плана**:

   * значения — случайные целые числа от **1500 до 9000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * сумму бонусов,
   * средний возраст.

---

## 🟦 Задание 2.8 (тематика: **Отдел снабжения**)

### Условие

1. Создайте DataFrame с данными сотрудников отдела снабжения:

   * ФИО;
   * Возраст;
   * Должность (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Должность** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Должности** (младший → старший),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Премия**:

   * значения — случайные целые числа от **2000 до 15000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * средний возраст,
   * сумму премий.

---

## 🟦 Задание 2.9 (тематика: **Отдел клиентской поддержки**)

### Условие

1. Создайте DataFrame с данными сотрудников клиентской поддержки:

   * ФИО;
   * Возраст;
   * Класс (категориальная переменная: "низший", "средний", "высший").
2. Преобразуйте столбец **Класс** в упорядоченную категориальную переменную с порядком: низший → средний → высший.
3. Отсортируйте сотрудников:

   * сначала по **Классу** (низший → высший),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Бонус за отзывы клиентов**:

   * значения — случайные целые числа от **1000 до 12000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * количество сотрудников каждого класса,
   * среднюю премию.

---

## 🟦 Задание 2.10 (тематика: **IT-поддержка**)

### Условие

1. Создайте DataFrame с данными сотрудников IT-поддержки:

   * ФИО;
   * Возраст;
   * Ранг (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Ранг** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Рангу** (младший → старший),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Премия за скорость обработки запросов**:

   * значения — случайные целые числа от **1500 до 10000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * минимальный и максимальный возраст,
   * среднее значение премии.

---

## 🟦 Задание 2.11 (тематика: **Отдел исследований**)

### Условие

1. Создайте DataFrame с данными сотрудников отдела исследований:

   * ФИО;
   * Возраст;
   * Статус (категориальная переменная: "аспирант", "младший исследователь", "старший исследователь").
2. Преобразуйте столбец **Статус** в упорядоченную категориальную переменную с порядком: аспирант → младший исследователь → старший исследователь.
3. Отсортируйте сотрудников:

   * сначала по **Статусу** (от аспиранта к старшему исследователю),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Премия за проект**:

   * значения — случайные целые числа от **1000 до 8000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * сумму премий,
   * средний возраст.

---

## 🟦 Задание 2.12 (тематика: **Логистика / транспортный отдел**)

### Условие

1. Создайте DataFrame с данными сотрудников транспортного отдела:

   * ФИО;
   * Возраст;
   * Категория водителя (категориальная переменная: "младший", "опытный", "ведущий").
2. Преобразуйте столбец **Категория водителя** в упорядоченную категориальную переменную с порядком: младший → опытный → ведущий.
3. Отсортируйте сотрудников:

   * сначала по **Категории водителя** (младший → ведущий),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Премия за рейсы**:

   * значения — случайные целые числа от **2000 до 10000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * максимальную и минимальную премию,
   * средний возраст.

---

## 🟦 Задание 2.13 (тематика: **Отдел закупок**)

### Условие

1. Создайте DataFrame с данными сотрудников отдела закупок:

   * ФИО;
   * Возраст;
   * Уровень (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Уровень** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Уровню** (младший → старший),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Премия за экономию**:

   * значения — случайные целые числа от **1500 до 9000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * сумму премий,
   * минимальный и максимальный возраст.

---

## 🟦 Задание 2.14 (тематика: **Отдел кадров / рекрутинг**)

### Условие

1. Создайте DataFrame с данными сотрудников рекрутинга:

   * ФИО;
   * Возраст;
   * Ранг (категориальная переменная: "ассистент", "специалист", "руководитель").
2. Преобразуйте столбец **Ранг** в упорядоченную категориальную переменную с порядком: ассистент → специалист → руководитель.
3. Отсортируйте сотрудников:

   * сначала по **Рангу** (ассистент → руководитель),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Бонус за найм**:

   * значения — случайные целые числа от **1000 до 12000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * среднее значение возраста,
   * сумму бонусов.

---

## 🟦 Задание 2.15 (тематика: **Отдел маркетинга / SMM**)

### Условие

1. Создайте DataFrame с данными сотрудников SMM:

   * ФИО;
   * Возраст;
   * Уровень (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Уровень** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Уровню** (младший → старший),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Премия за кампании**:

   * значения — случайные целые числа от **2000 до 15000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * максимальный и минимальный бонус,
   * средний возраст.

---

## 🟦 Задание 2.16 (тематика: **Отдел исследований / аналитика**)

### Условие

1. Создайте DataFrame с данными аналитиков:

   * ФИО;
   * Возраст;
   * Уровень (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Уровень** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Уровню** (младший → старший),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Премия за отчёты**:

   * значения — случайные целые числа от **1000 до 10000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * сумму премий,
   * средний возраст.

---

## 🟦 Задание 2.17 (тематика: **Отдел IT / DevOps**)

### Условие

1. Создайте DataFrame с данными сотрудников DevOps:

   * ФИО;
   * Возраст;
   * Ранг (категориальная переменная: "junior", "middle", "senior").
2. Преобразуйте столбец **Ранг** в упорядоченную категориальную переменную с порядком: junior → middle → senior.
3. Отсортируйте сотрудников:

   * сначала по **Рангу** (junior → senior),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Премия за uptime**:

   * значения — случайные целые числа от **1500 до 12000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * максимальный и минимальный ранг,
   * среднюю премию.

---

## 🟦 Задание 2.18 (тематика: **Отдел логистики / склад**)

### Условие

1. Создайте DataFrame с данными сотрудников склада:

   * ФИО;
   * Возраст;
   * Класс (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Класс** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Классу** (младший → старший),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Премия за обработку**:

   * значения — случайные целые числа от **1000 до 9000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * сумму премий,
   * минимальный возраст.

---

## 🟦 Задание 2.19 (тематика: **Отдел снабжения / закупки**)

### Условие

1. Создайте DataFrame с данными сотрудников отдела закупок:

   * ФИО;
   * Возраст;
   * Ранг (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Ранг** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Рангу** (младший → старший),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Бонус за экономию**:

   * значения — случайные целые числа от **2000 до 12000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * максимальный и минимальный бонус,
   * среднюю премию.

---

## 🟦 Задание 2.20 (тематика: **Отдел исследований / аналитики**)

### Условие

1. Создайте DataFrame с данными сотрудников аналитики:

   * ФИО;
   * Возраст;
   * Уровень (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Уровень** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Уровню** (младший → старший),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Премия за отчёты**:

   * значения — случайные целые числа от **1000 до 10000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * минимальный и максимальный возраст,
   * среднюю премию.

---

## 🟦 Задание 2.21 (тематика: **Отдел продаж / корпоративные клиенты**)

### Условие

1. Создайте DataFrame с данными сотрудников отдела продаж корпоративных клиентов:

   * ФИО;
   * Возраст;
   * Ранг (категориальная переменная: "младший менеджер", "менеджер", "старший менеджер").
2. Преобразуйте столбец **Ранг** в упорядоченную категориальную переменную с порядком: младший менеджер → менеджер → старший менеджер.
3. Отсортируйте сотрудников:

   * сначала по **Рангу** (младший → старший),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Бонус за продажи**:

   * значения — случайные целые числа от **2000 до 15000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * сумму бонусов,
   * средний возраст.

---

## 🟦 Задание 2.22 (тематика: **Отдел IT / разработка**)

### Условие

1. Создайте DataFrame с данными сотрудников IT-разработки:

   * ФИО;
   * Возраст;
   * Уровень (категориальная переменная: "junior", "middle", "senior").
2. Преобразуйте столбец **Уровень** в упорядоченную категориальную переменную с порядком: junior → middle → senior.
3. Отсортируйте сотрудников:

   * сначала по **Уровню** (junior → senior),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Премия за проект**:

   * значения — случайные целые числа от **1500 до 12000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * максимальный возраст,
   * минимальный возраст.

---

## 🟦 Задание 2.23 (тематика: **Отдел маркетинга / реклама**)

### Условие

1. Создайте DataFrame с данными сотрудников отдела рекламы:

   * ФИО;
   * Возраст;
   * Категория (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Категория** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Категории** (младший → старший),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Премия за кампании**:

   * значения — случайные целые числа от **1000 до 10000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * сумму премий,
   * средний возраст.

---

## 🟦 Задание 2.24 (тематика: **Отдел кадров / обучение**)

### Условие

1. Создайте DataFrame с данными сотрудников отдела обучения:

   * ФИО;
   * Возраст;
   * Уровень (категориальная переменная: "стажёр", "специалист", "руководитель").
2. Преобразуйте столбец **Уровень** в упорядоченную категориальную переменную с порядком: стажёр → специалист → руководитель.
3. Отсортируйте сотрудников:

   * сначала по **Уровню** (стажёр → руководитель),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Бонус за обучение**:

   * значения — случайные целые числа от **1000 до 8000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * максимальный бонус,
   * средний возраст.

---

## 🟦 Задание 2.25 (тематика: **Отдел логистики / склад**)

### Условие

1. Создайте DataFrame с данными сотрудников склада:

   * ФИО;
   * Возраст;
   * Ранг (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Ранг** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Рангу** (младший → старший),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Премия за обработку**:

   * значения — случайные целые числа от **1500 до 10000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * сумму премий,
   * минимальный возраст.

---

## 🟦 Задание 2.26 (тематика: **Отдел исследований / лаборатория**)

### Условие

1. Создайте DataFrame с данными сотрудников лаборатории:

   * ФИО;
   * Возраст;
   * Степень (категориальная переменная: "ассистент", "младший научный сотрудник", "старший научный сотрудник").
2. Преобразуйте столбец **Степень** в упорядоченную категориальную переменную с порядком: ассистент → младший научный сотрудник → старший научный сотрудник.
3. Отсортируйте сотрудников:

   * сначала по **Степени** (ассистент → старший научный сотрудник),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Премия за публикации**:

   * значения — случайные целые числа от **2000 до 15000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * максимальный возраст,
   * сумму премий.

---

## 🟦 Задание 2.27 (тематика: **Отдел продаж / интернет-магазин**)

### Условие

1. Создайте DataFrame с данными сотрудников интернет-магазина:

   * ФИО;
   * Возраст;
   * Уровень (категориальная переменная: "младший менеджер", "менеджер", "старший менеджер").
2. Преобразуйте столбец **Уровень** в упорядоченную категориальную переменную с порядком: младший менеджер → менеджер → старший менеджер.
3. Отсортируйте сотрудников:

   * сначала по **Уровню** (младший → старший),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Бонус за продажи**:

   * значения — случайные целые числа от **1000 до 12000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * максимальную премию,
   * средний возраст.

---

## 🟦 Задание 2.28 (тематика: **Отдел IT / поддержка**)

### Условие

1. Создайте DataFrame с данными сотрудников IT-поддержки:

   * ФИО;
   * Возраст;
   * Ранг (категориальная переменная: "junior", "middle", "senior").
2. Преобразуйте столбец **Ранг** в упорядоченную категориальную переменную с порядком: junior → middle → senior.
3. Отсортируйте сотрудников:

   * сначала по **Рангу** (junior → senior),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Премия за обращения**:

   * значения — случайные целые числа от **1500 до 10000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * минимальный возраст,
   * сумму премий.

---

## 🟦 Задание 2.29 (тематика: **Отдел маркетинга / PR**)

### Условие

1. Создайте DataFrame с данными сотрудников PR-отдела:

   * ФИО;
   * Возраст;
   * Категория (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Категория** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Категории** (младший → старший),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Бонус за PR-кампании**:

   * значения — случайные целые числа от **1000 до 12000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * средний возраст,
   * максимальную премию.

---

## 🟦 Задание 2.30 (тематика: **Отдел снабжения / логистика**)

### Условие

1. Создайте DataFrame с данными сотрудников отдела снабжения:

   * ФИО;
   * Возраст;
   * Уровень (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Уровень** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Уровню** (младший → старший),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Премия за логистику**:

   * значения — случайные целые числа от **2000 до 12000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * минимальный возраст,
   * средний бонус.

---

## 🟦 Задание 2.31 (тематика: **Отдел исследований / аналитика**)

### Условие

1. Создайте DataFrame с данными сотрудников аналитического отдела:

   * ФИО;
   * Возраст;
   * Статус (категориальная переменная: "ассистент", "аналитик", "старший аналитик").
2. Преобразуйте столбец **Статус** в упорядоченную категориальную переменную с порядком: ассистент → аналитик → старший аналитик.
3. Отсортируйте сотрудников:

   * сначала по **Статусу** (ассистент → старший аналитик),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Бонус за отчёты**:

   * значения — случайные целые числа от **1000 до 10000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * средний бонус,
   * минимальный возраст.

---

## 🟦 Задание 2.32 (тематика: **Отдел кадров / рекрутинг**)

### Условие

1. Создайте DataFrame с данными сотрудников рекрутинга:

   * ФИО;
   * Возраст;
   * Уровень (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Уровень** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Уровню** (младший → старший),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Премия за наймы**:

   * значения — случайные целые числа от **1500 до 12000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * максимальный возраст,
   * сумму премий.

---

## 🟦 Задание 2.33 (тематика: **Отдел логистики / транспорт**)

### Условие

1. Создайте DataFrame с данными сотрудников транспортного отдела:

   * ФИО;
   * Возраст;
   * Ранг (категориальная переменная: "водитель-новичок", "водитель", "старший водитель").
2. Преобразуйте столбец **Ранг** в упорядоченную категориальную переменную с порядком: водитель-новичок → водитель → старший водитель.
3. Отсортируйте сотрудников:

   * сначала по **Рангу** (новичок → старший),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Премия за рейсы**:

   * значения — случайные целые числа от **2000 до 10000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * сумму премий,
   * максимальный возраст.

---

## 🟦 Задание 2.34 (тематика: **Отдел IT / разработка веб-сайтов**)

### Условие

1. Создайте DataFrame с данными сотрудников веб-разработки:

   * ФИО;
   * Возраст;
   * Уровень (категориальная переменная: "junior", "middle", "senior").
2. Преобразуйте столбец **Уровень** в упорядоченную категориальную переменную с порядком: junior → middle → senior.
3. Отсортируйте сотрудников:

   * сначала по **Уровню** (junior → senior),
   * затем по **Возрасту** по возрастанию.
4. Добавьте столбец **Премия за проекты**:

   * значения — случайные целые числа от **2000 до 15000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * последние 5 строк таблицы,
   * средний возраст,
   * среднюю премию.

---

## 🟦 Задание 2.35 (тематика: **Отдел маркетинга / контент**)

### Условие

1. Создайте DataFrame с данными сотрудников контент-отдела:

   * ФИО;
   * Возраст;
   * Ранг (категориальная переменная: "младший", "средний", "старший").
2. Преобразуйте столбец **Ранг** в упорядоченную категориальную переменную с порядком: младший → средний → старший.
3. Отсортируйте сотрудников:

   * сначала по **Рангу** (младший → старший),
   * затем по **Возрасту** по убыванию.
4. Добавьте столбец **Бонус за контент**:

   * значения — случайные целые числа от **1000 до 12000**;
   * тип данных должен быть int.
5. После сортировки:

   * сбросьте индекс,
   * удалите старые значения индексов.
6. Выведите:

   * первые 5 строк таблицы,
   * максимальный бонус,
   * минимальный возраст.

